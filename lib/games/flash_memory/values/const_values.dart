class Consts {
  static const Duration showDuration = Duration(milliseconds: 500);
  static const Duration cardDisplayDuration = Duration(milliseconds: 4500);
  static const Duration cardAnimationDuration = Duration(milliseconds: 100);
}
